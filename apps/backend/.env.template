STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa_sng

# SMTP configuration for transactional emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Gorgo Store <noreply@example.com>"
SMTP_REPLY_TO=

# Storefront URL (used in email links)
STOREFRONT_URL=https://localhost:8000
STOREFRONT_DEFAULT_COUNTRY=ru

# Email i18n: default locale for emails when country cannot be determined (ru or en)
STOREFRONT_DEFAULT_LOCALE=ru

# Store branding used in email templates
STORE_NAME=GORGO MEDUSA STORE
STORE_EMAIL=demo@gorgojs.com
STORE_PHONE=+7 111 11-11-11
STORE_ADDRESS=Москва, Россия
